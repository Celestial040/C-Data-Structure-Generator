#ifndef CODEGEN_H
#define CODEGEN_H

#include "status.h"

typedef enum TargetDataStructure {
    STATIC_ARRAY,
    DYNAMIC_ARRAY,
    LINKED_LIST,
    HASHMAP,
    BINARY_TREE
} TargetDataStructure;


#endif
